name: node_protocol
description: Shared protocol models for Family Home Agent room nodes.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
